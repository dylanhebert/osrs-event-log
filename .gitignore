venv/
.venv/
*.log
__pycache__/
bot_config.json
db_discord.json
db_runescape.json
sotw_config.json
botw_config.json
requirements_old.txt

# SQLite state. Same rule as the JSON files it replaces: the droplet holds the
# only copy, backed up by gd-ops/scripts/backup-db.sh.
*.db
*.db-wal
*.db-shm

# Local copies of live data pulled from the droplet for testing.
fixtures/

# Web UI secrets: SECRET_KEY, the database path, and the guild-id-to-name map.
# web/.env.example is the tracked template.
web/.env